# Fixtures

Run100's original report records a real H264/AAC fixture/export validation, but the historical binary fixture was not recovered into the official repository during P0 recovery.

Do not fabricate it. Add a deterministic local fixture only in a later, separately validated reliability run.
